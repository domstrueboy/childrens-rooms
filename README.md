# childrens-rooms

Запускаем контейнер

cd backend
docker-compose build
docker-compose up
Go to http://localhost:81/
Expected result: Hoy!
Go to localhost:81/api/ping 
Expected result: {"message":"pong"}

